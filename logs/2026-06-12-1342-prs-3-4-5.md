# 2026-06-12 · 13:42 BST — merged PRs #3, #4, #5

`main` → `e17e270`. No conflicts; gate green (tsc + lint + smoke).

- **PR #3** — data dedupe: `offers.ts` is the canonical catalogue, `product.ts`
  now derives from it (importers unchanged). Closes the dupe.
- **PR #4** — **client booking flow** (`/client/book/*`: business profile →
  services → professional → time → review → confirmed); new `clientApp.ts` data +
  `clientBooking.ts` store; gated in smoke.
- **PR #5** — AppHeader avatar links to the business hub.

Audit notes: the three touched no shared files → independent, clean merges.
**PR #1 & #2** were already in `main` (merged earlier) — lingering "open" on
GitHub; should auto-close on this push, else close manually.
